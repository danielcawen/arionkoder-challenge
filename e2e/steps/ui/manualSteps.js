import { Then } from '@cucumber/cucumber'

Then('the heading, subtitle and button should be visually aligned', async function () {
  return 'pending'
})

Then('the colour scheme and typography should match the design spec', async function () {
  return 'pending'
})
