{
  "name": "arionkoder-challenge-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npx cucumber-js",
    "test:ui": "npx cucumber-js --profile ui",
    "test:manual": "npx cucumber-js --profile manual",
    "test:rerun": "npx cucumber-js @rerun.txt --profile rerun",
    "format": "npx prettier --write \"e2e/**/*.js\""
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^13.0.0",
    "@playwright/test": "^1.61.0",
    "dotenv": "^17.4.2",
    "prettier": "^3.0.0"
  }
}
